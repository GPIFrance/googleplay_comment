<script>
	window.environment = {
		{% if app.user %}
		user: {
			username: '{{ app.user.username }}',
			roles: {{ app.user.roles|json_encode(constant('JSON_UNESCAPED_UNICODE'))|raw }}
		},
		{% endif %}
		routing: Routing
	};
</script>